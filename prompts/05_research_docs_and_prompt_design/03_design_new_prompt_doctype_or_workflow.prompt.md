# Research Docs and Prompt Design > Design New Prompt Doctype or Workflow
@workflow: simple_prompt_generator
@doctype: 01_normal_doctype
@bucket: Research Docs and Prompt Design

## Task
Design a new reusable prompt, doctype, or workflow for this app. First explain the job it is meant to do, what defaults should be checked, what should stay optional, and how it differs from the existing library. Then write the actual MD so it is ready to drop into the project.

## Context
[comment]
Use this when the library needs a new building block instead of another one-off chat prompt.
[/comment]

Aim for reusable, battle-tested wording instead of clever filler.
