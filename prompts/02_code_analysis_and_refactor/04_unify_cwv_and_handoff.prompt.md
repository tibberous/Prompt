# Code Analysis and Refactor > Unify CWV and Handoff
@workflow: simple_prompt_generator
@doctype: 01_normal_doctype
@bucket: Code Analysis and Refactor

## Task
Unify all code into one complete canonical working version. Delete drafts, scratch blocks, duplicate functions, and superseded files. The output should be a clean CWV — no part 1 / part 2, no commented-out old version, no stale branch copies. Then create the full zip handoff and tell me exactly what was not finished.

## Context
[comment]
Use this right before delivery or when the workspace has started drifting into branch soup.
[/comment]

If runtime proof happened on a pre-cleanup branch, repeat the final sanity check on the unified CWV before handoff.
